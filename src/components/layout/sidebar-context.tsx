/**
 * Re-export barrel — canonical location is now @/lib/providers/sidebar-context
 * This file is kept for backward compatibility during the migration.
 * Update your imports to: @/lib/providers/sidebar-context
 */
export { SidebarProvider, useSidebar } from "@/lib/providers/sidebar-context";
export type { SidebarContextType } from "@/lib/providers/sidebar-context";
